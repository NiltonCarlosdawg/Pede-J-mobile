# O projeto tem conflitos de peer deps conhecidos (@config-plugins/react-native-callkeep
# e lottie-react-native contra o Expo SDK 57). Fixar a resolução em "legacy" mantém
# `npm install` e `npm ci` idênticos entre a máquina local e o CI.
legacy-peer-deps=true
